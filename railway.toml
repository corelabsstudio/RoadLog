# Railway — Dockerfile only. Do NOT set healthcheck here.
# If dashboard still shows Healthcheck failure:
#   Service → Settings → Deploy → Healthcheck Path → clear/empty → Save → Redeploy

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
