# Railway — Dockerfile 빌드
# https://docs.railway.app

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Dockerfile CMD 사용 (중복 startCommand 제거로 혼란 방지)
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
